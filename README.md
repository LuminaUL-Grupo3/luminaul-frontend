
  # [LOU] LuminaUL (Copia)

  This is a code bundle for [LOU] LuminaUL (Copia). The original project is available at https://www.figma.com/design/nXGKi8wyPUt7RHdtOYmvbZ/-LOU--LuminaUL--Copia-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  